server:
  port: 8080
  host: "0.0.0.0"

database:
  domains_db: "/app/data/domains.db"
  stats_db: "/app/data/stats.db"

logging:
  level: "info"

cors:
  allowed_origins:
    - "http://localhost:8080"
    - "http://localhost:5173"
  allow_credentials: true
