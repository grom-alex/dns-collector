# PostgreSQL Configuration
POSTGRES_USER=dns_collector
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
POSTGRES_DB=dns_collector

# Data Storage Path
# Absolute path on host where PostgreSQL data will be stored
# Default: uses Docker volume (recommended)
# Custom path example: /var/lib/dns-collector/postgres
POSTGRES_DATA_PATH=

# Timezone
TZ=UTC

# Notes:
# 1. Copy this file to .env and update with actual values
# 2. NEVER commit .env file to git
# 3. Use strong passwords in production
# 4. Consider using Docker secrets for sensitive data
